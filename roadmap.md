# Roadmap - CoT Loop Detection

Last updated: 2026-04-09 01:40 UTC

Scope:
- Build and validate a probe pipeline for CoT loop detection across prefill and completion feature views.
- Quantify generalization across natural eval splits and OOD tasks.

## Current Status
- Milestone 1 gate: complete.
- Milestone 2 gate: complete.
- Milestone 3 gate: complete.
- Active milestone: Milestone 5 (deployment readiness).
- Latest result: the collaborator-facing prompt-profile surface now has a dedicated mechanism note, `docs/prompt-profile-metadata-mechanism-2026-04-06.md`, plus `outputs/prompt_profile_metadata_mechanism_20260406/`. That note answers the narrower question the combined audit only partially resolved: why the prompt-only predictors are strong. The key correction is that both current targets are mostly fixed-budget long-completion objects, and `majority_s_0.5` is largely downstream of `mean_relative_length`, so prompt-family surface cues naturally transfer to both heads.
- Prompt-profile direction: binary `majority_s_0.5` still has the cleaner deployment-facing ranking signal, and the current best global ensemble default on that balanced binary surface is still `h256 d1`. But the mechanism note narrows the next honest experiment further than "stronger metadata baseline": the cheapest decisive follow-up is now a residualized conditional-lift audit on the natural regression head, because the binary head is too downstream of `mean_relative_length` to cleanly separate long-completion risk from loop-specific signal.
- Parallel OLMo direction: the audit object is now materially closed rather than only queued. The suspect bounded OLMo3 `MMLU-Pro` and `LiveCodeBench` rows have been repaired on the saved outputs, the corrected result surface is packaged in `docs/olmo-degeneration-origin-audit-2026-04-04.md` plus `outputs/olmo_degeneration_origin_audit_20260404/`, and the larger OLMo2 `1B` rerun at `50` prompts per dataset is now the right stage-conclusion surface rather than the old pilot. That larger rerun keeps the main base-origin read but makes the later-stage story more mixed: base is still much worse, SFT still carries residual degeneration, `RLVR1` is usually the cleanest point, and final instruct can re-accumulate on harder datasets. The new figure bundle under `outputs/olmo2_1b_progression_bound50_20260406/` makes that stage shape visible directly. The Qwen same-family follow-up is finished now too, not just mid-collection: the canonical local bundle is `outputs/qwen3_1p7b_base_raw_control_finished_20260409/`, and the combined collaborator-facing read now lives in `docs/olmo-qwen-degeneration-origin-combined-2026-04-09.md` plus `outputs/olmo_qwen_degeneration_combined_20260409/`. Under the old v2 sampler, Qwen base raw is worse than the saved instruct reference on loop fraction and max-length-hit fraction for all five datasets, with the sharpest gaps on `GPQA`, `MMLU-Pro`, and `LiveCodeBench`. The old `LiveCodeBench` LM-style caveat still matters, but the tiny style probe already showed it is not the easy rescue: both `GenericBase` and `CodeQwenInstruct` looped to the cap on the same first `2` prompts. The cross-family split in failure mode still holds: OLMo2 base repeats content-level derivations and synthetic junk, while Qwen base raw often loops by repeating the answer-format instruction tail on MCQ.
- Runtime status: the resumed GPQA prompt-profile pilot (`1477`) is now complete rather than pending. It finished in `37m22s` on GPUs `0-2`, wrote the full prompt-profile archive plus probe checkpoints, and showed that the 3-GPU runtime path is viable. The blocker is now objective usefulness, not Slurm/runtime feasibility.
- Active experiment state: the standardized same-archive table is now complete for all five datasets (`GPQA`, `AIME`, `MATH-500`, `MMLU-Pro`, and `LiveCodeBench`). The fifth dataset did not change the retrospective ranking: `mean_relative_length` remains the strongest current regression head on that bundle, `p_loop` remains the loop-specific analysis head, and `loop_budget_share` stays auxiliary-only.
- Evaluation contract: the docs still keep the baseline terminology explicit. For binary `majority_s_0.5`, prompt length means a true one-feature held-out scorer with train-chosen direction and threshold. For the continuous heads, the old raw prompt-length-only association table has now been superseded by the first real metadata-only control suite in `outputs/prompt_profile_risk_controls_20260330/`: logistic metadata baselines for `p_loop`, linear metadata baselines for `mean_relative_length`, and the direct held-out top-risk bucket comparison on actual loop rate, cap-hit rate, and accuracy where available. That bundle is now interpreted in two layers: held-out target fit chooses the objective, while the bucket slice stays only as a downstream diagnostic.
- Open items moved to backlog.md.

## Milestone 1 - Pipeline and multi-view infrastructure
Status: done (2026-03-05 18:45 UTC)
Success criteria:
- Dataset builder supports prefill and completion feature views in a single pass.
- Training and eval support explicit feature-key selection, multi-view reuse, and imbalance-aware metrics.
- Slurm launchers exist for shared-dataset and k=5 three-view sweeps.

## Milestone 2 - Completion-vs-prefill baseline package
Status: done (2026-03-05 18:45 UTC)
Success criteria:
- Consolidated findings doc and PDF reflect the k=5 three-view results.
- PR #2 includes the stacked all-layer prefill, metrics, and metadata follow-up code paths.
- Completion-view and prefill-view performance are directly comparable on shared labels.

## Milestone 3 - Metadata-aware prefill residual validation
Status: done (2026-03-13 21:44 UTC)
Success criteria:
- Re-run prefill follow-ups under fixed labels / metadata-aware controls instead of raw composition drift.
- Determine whether any prompt-summary or augmentation beats the metadata-aware last-token anchor on matched evaluation.
- Record the best prefill candidate and its incremental lift versus both metadata-only and anchor-only baselines.

## Milestone 4 - Cross-dataset validation
Status: done (2026-03-30 03:10 UTC)
Success criteria:
- Replicate the current best prefill/completion findings on additional evaluation sets or model variants.
- Measure whether the preferred feature view survives varied token budgets and source mixes.
- Separate true robustness gains from prompt-length or source-composition effects.
- Establish a useful in-distribution prompt-level predictor objective for fixed model+policy behavior, with metadata-controlled evaluation.
- Make that objective less brittle to the chosen decode ceiling than a pure max-length-hit label.

Activity log:
- 2026-04-09 01:40 UTC: closed the parked Qwen follow-up instead of leaving the project state frozen at the last live checkpoint. I re-entered `tianhaowang-gpu0`, confirmed there were no remaining collector processes, and copied the finished JSONs for `AIME`, `GPQA`, `MMLU-Pro`, and `LiveCodeBench` into the local project output bundle `outputs/qwen3_1p7b_base_raw_control_finished_20260409/` alongside the earlier `MATH-500` row. That turns the Qwen same-family control into a real five-dataset object rather than one durable row plus four liveness receipts. The finished base/raw rows are all visibly degenerate: `AIME 114 / 500` looped and `116 / 500` max-hit, `GPQA 190 / 500` and `190 / 500`, `MMLU-Pro 157 / 500` and `157 / 500`, `LiveCodeBench 232 / 500` and `234 / 500`. I also rebuilt a local Qwen summary CSV/JSON, generated comparison plots against the saved instruct-side v2 reference, and wrote the new combined OLMo+Qwen note/PDF surface. The clean read is stronger than the earlier half-finished checkpoint: the base/raw pathology is not OLMo-only, but it is still not the same failure object across families.
- 2026-04-06 09:25 UTC: the narrow `LiveCodeBench` wrapper question is no longer hypothetical. I finished the tiny `Qwen/Qwen3-1.7B-Base` style probe on the same first `2` `release_v6` prompts under the same long-horizon sampler. Both `LM_STYLE=GenericBase` and `LM_STYLE=CodeQwenInstruct` came back at `2 / 2` looped, `2 / 2` max-length-hit, `pass@1 = 0.0`. The difference is text behavior, not the top-line outcome: `GenericBase` starts with plausible code and then drifts into repeated problem text, while `CodeQwenInstruct` collapses almost immediately into repeated `# YOUR CODE HERE`. So the old-v2-vs-base `LiveCodeBench` LM-style mismatch remains a real provenance caveat, but it is not enough by itself to explain away the bad base `LiveCodeBench` behavior. I also refreshed the live node checkpoint again: only `MATH-500` is finished, and the remaining base collectors are now at `AIME 29 / 50`, `GPQA 23 / 50`, `MMLU-Pro 28 / 50`, and `LiveCodeBench 15 / 50`.
- 2026-04-04 23:48 UTC: Wangzhi corrected the regression training object again after the count-fix pass: `mean_relative_length` should use the natural distribution and natural sampler because it is not a binary-label problem. I did not rerun anything after that; the point was interpretive, not computational. I updated the README/backlog/roadmap surface so the canonical regression lane is once again the locked natural-split April run (`docs/prompt-profile-full-train-results-2026-04-04.md` and `docs/prompt-profile-full-surface-update-2026-04-04.md`). The two balanced-regression notes are now retained only as side analyses explaining the earlier wrong-object detour and the count correction, not as the main regression deliverable.
- 2026-04-04 23:12 UTC: corrected the balanced-only prompt-profile regression surface after Wangzhi's count objection. The earlier same-day note had reused the downsampled balanced binary subset as the regression train set, which meant `GPQA`, `MATH-500`, and `MMLU-Pro` were training on only `18`, `44`, and `14` prompts instead of the full saved Qwen3 counts `158`, `400`, and `640`. I reran the object as Slurm `2214` under `/data/scratch/murphy/outputs/cot-loop-detection/full_train_locked_pair_20260404_regression_balanced_sampler/`, keeping the full shared regression train/test splits and using the natural `majority_s_0.5` labels only for train-time sampler balancing. I also patched `scripts/run_prompt_profile_full_train.py` so future reruns leave a local `shared_archive` link when they read from an external source root; that keeps the summary path from dropping the metadata baselines. The corrected report now lives at `docs/prompt-profile-balanced-regression-corrected-2026-04-04.md` and `outputs/prompt_profile_balanced_regression_corrected_20260404/`. The final read changed materially: ensemble is still the best screening surface by `top_20p_capture` (`0.345` mean, wins `5 / 5` against last-layer and `4 / 5` against prompt length), but the corrected full-train prompt-length baseline is now the best `RMSE` surface on `4 / 5` datasets.
- 2026-04-04 22:54 UTC: closed the OLMo degeneration-origin audit as a real result surface instead of leaving it split between the working note and Slack. Two concrete repairs changed the bounded OLMo3 read: `_extract_json_answer_field` now accepts relaxed terminal forms such as `{"answer": I}`, which turns the old `RLVR / MMLU-Pro = 0 / 80` failure row into `27 / 80` correct with `0 / 80` loops / `0 / 80` max hits, and `livecodebench_codegen.py` now has an OLMo-native path instead of the old silent Qwen-wrapper fallback, which moves bounded OLMo3 `LiveCodeBench` to `6 / 80` for SFT with `1 / 80` loop / `1 / 80` max hit and `22 / 80` for RLVR with `0 / 80` loops / `0 / 80` max hits. I also pulled the exact remote JSON rows for the full bounded OLMo2 `1B` ladder and wrote the durable note/PDF pair at `docs/olmo-degeneration-origin-audit-2026-04-04.md` and `outputs/olmo_degeneration_origin_audit_20260404/olmo_degeneration_origin_audit_20260404.pdf`. That fallback now shows the stage pattern directly: base is heavily degenerate on all five datasets, SFT is still materially degenerate on several datasets, and `RLVR1` / instruct are much cleaner on loop / max-length mass even when some bounded rows remain low-accuracy.
- 2026-04-04 21:41 UTC: closed the actual missing April prompt-profile object instead of leaving "balanced regression" as a blocked rerun. After `tianhaowang-gpu0` started accepting normal SSH again, I submitted the balanced-train / natural-test `mean_relative_length` rerun as Slurm `2142` under `/data/scratch/murphy/outputs/cot-loop-detection/full_train_locked_pair_20260404_regression_balanced/`, reusing the saved balanced `majority_s_0.5` prompt IDs from the locked April root. Two code fixes landed during that execution: the Slurm wrappers now honor explicit `CONDA_ENV` before any stale repo-local `.venv`, and `scripts/summarize_prompt_profile_full_train.py` now summarizes regression-only reruns instead of labeling them `missing_shared_archive`. The finished balanced-regression note/PDF plus copied summary ledger now live under `docs/prompt-profile-balanced-regression-2026-04-04.md` and `outputs/prompt_profile_balanced_regression_20260404/`. The final read is sharper than the earlier mixed April note: on held-out `top_20p_capture`, ensemble beats prompt length on `4 / 5` datasets (`GPQA`, `MATH-500`, `MMLU-Pro`, `LiveCodeBench`) and beats last-layer on `4 / 5`, but on `RMSE` the cheaper `last_layer` control still has the better cross-dataset mean (`0.191` versus `0.205`).
- 2026-04-04 00:17 UTC: closed the actual backlog item instead of leaving it as a runnable plan. I pulled the finished `2043` ledger off the GPU-node scratch runtime checkout, copied the summary JSON/CSV plus Slurm log into `outputs/prompt_profile_full_train_locked_pair_20260404/`, and wrote the durable result note/PDF. The finished read is narrower than the early Slack checkpoints suggested: on regression `mean_relative_length`, ensemble beats `last_layer` on all five datasets but only beats the train-fit prompt-length baseline on `GPQA` and `LiveCodeBench`; on binary `majority_s_0.5`, ensemble `PR-AUC` beats prompt length on all five datasets, with the cleanest finished wins on `AIME`, `LiveCodeBench`, and `MMLU-Pro`. One interpretability correction also became explicit in the note: the regression `ensemble` surface is not majority vote; it is one MLP per layer with `mean_prob` aggregation, while the binary head uses per-layer vote averaging via `vote_fraction`.
- 2026-04-03 23:17 UTC: turned the locked full-train note into an executable project surface instead of leaving it as a manual command chain. The repo now has `scripts/run_prompt_profile_full_train.py`, which freezes the five-dataset build / regression-train / relabel / binary-train path for the locked `mean_relative_length` / `majority_s_0.5` pair under the saved `Qwen/Qwen3-1.7B`, `temperature=0.2`, `num_generations=4`, `loop_n=30`, `loop_k=20` contract. It also has `scripts/summarize_prompt_profile_full_train.py`, which reads the finished run roots, aggregates `best_loss` and `best_rank` across seeds for ensemble and last-layer, and computes the required metadata baselines (`prompt_length`, `effective_budget`, and the joint regression control). That makes the next backlog item an actual runnable object instead of another thread-only recipe.
- 2026-04-02 23:04 UTC: wrote the missing full-train execution note and companion PDF for the now-locked prompt-profile target pair. The new canonical objects are `docs/prompt-profile-full-train-plan-2026-04-02.md` and `outputs/prompt_profile_full_train_plan_20260402/prompt_profile_full_train_plan_20260402.pdf`. The point of that note is to stop another thread-level drift before execution starts: it freezes the actual run contract instead of only restating the target choice. The plan keeps the current saved prompt-profile surface unchanged (`Qwen/Qwen3-1.7B`, `temperature=0.2`, `num_generations=4`, `loop_n=30`, `loop_k=20`, prompt-prefill only), uses `mean_relative_length` as the shared archive build target, relabels the same archive to `majority_s_0.5`, treats ensemble as the main view with last-layer as the cheap control, and keeps `best_loss` as the main checkpoint while leaving `best_rank` diagnostic-only. It also makes the remaining TO DOs explicit: run the full pass on the locked pair, keep metadata baselines mandatory, and keep `p_loop` / bucket concentration in the diagnostic lane instead of letting them re-open target choice.
- 2026-04-02 11:34 UTC: maintenance rechecked the live PR surface after yesterday's target lock. The execution story did not change: Wangzhi's requested next-run pair is still regression `mean_relative_length` plus binary `majority_s_0.5`, and the immediate deliverable is still the PDF train-run plan for that pair. What changed is the review diagnosis: upstream PR `#7` is now at head `6edc889` with `16` unresolved non-outdated review threads, and the old fork-base blocker is now obsolete because `review_project.sh --base main` synced against the correct `upstream/main` base in this pass. The branch is still not locally review-cleared because the bounded review timed out again before a terminal verdict.
- 2026-04-01 11:18 UTC: the collaborator froze the next execution surface instead of leaving target choice open. Wangzhi explicitly locked `mean_relative_length` plus `majority_s_0.5` as the two train targets for the next full run and asked for the train-run plan as a PDF deliverable. I refreshed the README/backlog/docs index around that instruction so the repo no longer frames the next step as another open objective-selection debate. The PR surface itself did not improve in this pass: upstream PR `#7` is still at `211e177` with `16` unresolved non-outdated review threads, and the maintenance review wrapper still cannot produce a trustworthy local verdict because it assumes `origin/main` instead of the real `upstream/main` base.
- 2026-03-30 03:10 UTC: closed the main unresolved prompt-profile question with one reusable analysis pass instead of another thread-level argument. I added `scripts/analyze_prompt_profile_risk_controls.py`, synced the current repo into a throwaway remote checkout, and ran it against the finished saved bundles on `tianhaowang-gpu0`, writing the results to `outputs/prompt_profile_risk_controls_20260330/`. That pass did two things the old thread still lacked: (1) trained the first true metadata-only continuous baselines for `p_loop` and `mean_relative_length` on the held-out prompt-disjoint splits (`prompt_length`, `effective_budget`, and their joint control), and (2) ran the top-risk-20% bucket test on actual prompt-level loop rate, max-length-hit rate, and accuracy where available. The result is finally decisive enough to freeze the objective: `p_loop` wins loop-rate enrichment on `AIME`, `MATH-500`, `MMLU-Pro`, and `LiveCodeBench`, roughly ties `GPQA` with `majority_s_0.5`, and gives the worst-accuracy bucket on all four datasets with prompt-level correctness. Averaged across datasets, loop-rate enrichment is about `2.86x` for `p_loop`, versus `1.87x` for `majority_s_0.5` and `1.24x` for the strongest metadata baseline. So the project objective is no longer "two equal heads by default": `p_loop` is the main screening target, `mean_relative_length` is the secondary utility head, `majority_s_0.5` is a control, and direct `p_cap` does not need to be reopened right now. One caveat stays open in the artifact itself: `LiveCodeBench` still lacks prompt-level accuracy in the recovered projection export, so the low-accuracy bucket claim is `4 / 5` datasets rather than `5 / 5`.
- 2026-03-25 10:02 UTC: rebuilt the explicit cross-dataset `majority_s_0.5` scoreboard from the finished node artifacts instead of another thread-level paraphrase. The local summarizer had to be fixed first because the current surface is not uniform: the `MATH-500` projection directory uses `math500` while the probe runs use `math`, and the finished `MATH-500` export was backfilled one day before the saved probe metrics, so exact `(dataset, seed, date)` joins were silently dropping that dataset. `scripts/summarize_prompt_majority_pilot.py` now canonicalizes dataset aliases, tracks projection and probe date tags separately, records the full binary metric bundle for the prompt-length and effective-budget baselines, and emits both raw per-seed rows and dataset-aggregated summaries. I ran that refreshed summarizer against `/data/scratch/murphy/outputs/cot-loop-detection` on `tianhaowang-gpu0` and copied the rebuilt artifacts back into `outputs/prompt_majority05_cross_dataset_rebuild_20260325/`. The clean result closes the first logged handoff item: prompt length is not a global answer on `majority_s_0.5`; it is strong on `AIME`, moderate on `LiveCodeBench`, and weak on `GPQA` / `MATH-500` / `MMLU-Pro`. The actual remaining proof burden is now exactly what the collaborator asked for: the bucket test on empirical loop rate, max-length-hit rate, and accuracy, plus the first real metadata-only continuous baselines.
- 2026-03-25 06:10 UTC: wrote a durable thread-reset handoff after the collaborator asked for the recent messages to be summarized into the project so a new Slack thread could start cleanly. The new note (`docs/thread-reset-2026-03-25.md`) records the collaborator's corrections in plain terms: stop treating raw prompt-length correlation as a trained baseline, stop leaning on `AIME` plus `PR-AUC` alone, keep the project objective on catching degenerate rollouts, and close the proved-vs-unproved ledger before returning to head recommendations. The exact next work order is now frozen in docs too: (1) rebuild the cross-dataset `majority_s_0.5` scoreboard with prompt-length-only and learned probes side by side plus full binary metrics, (2) run the first true metadata-only continuous-head baselines, and (3) run the held-out top-risk-bucket comparison on empirical loop rate, max-length-hit rate, and accuracy before making any stronger operational claim.
- 2026-03-25 05:26 UTC: consulted Athena again in `deep` mode specifically on the collaborator's fundamentals-first question: what is actually proven today about prompt length and whether `majority_s_0.5` catches degenerate rollouts across datasets. Athena did not change the local recommendation surface. The useful sharpening was the ledger: prompt length is only truly proven for binary `majority_s_0.5`, and even there the result is dataset-conditional (`GPQA` weak, `AIME` strong); the hard-dataset rollout stats prove the degenerate regime is real, but the threshold label still is not justified as the cross-dataset operational screen until the held-out top-risk-bucket comparison is run against actual loop rate, actual max-length-hit rate, and actual accuracy. Consult log: `.agent/runtime/consult_history/1773870804.162569.jsonl`.
- 2026-03-25 04:41 UTC: collaborator feedback showed that the earlier docs fix was still too easy to misread. I tightened the wording again so the baseline object is now algorithmically explicit instead of just described loosely: for binary `majority_s_0.5`, the prompt-length baseline is a train-fit 1D rule on `prompt_token_count` with train-chosen direction and threshold, then held-out evaluation; for the current continuous-head table, the saved prompt-length number is only raw held-out `Spearman(prompt_length, target)` and should not be described as a trained metadata predictor. The backlog now treats the first real continuous-head metadata-model pass as unfinished work, not as something already implied by the saved table.
- 2026-03-25 00:56 UTC: clarified the interpretation contract after collaborator feedback. The docs now answer five operational questions directly instead of spreading them across thread notes: (1) for binary `majority_s_0.5`, "prompt length alone" already means a real held-out 1D scorer with train-chosen orientation and threshold; (2) for the five-dataset continuous-head table, prompt length is still only raw held-out association and is not yet a trained predictor; (3) the strong `majority_s_0.5` prompt-length baseline does **not** kill that target operationally, but it does keep it out of the main activation-lift claim; (4) `Spearman` is now spelled out as prompt-level rank agreement with the realized target; and (5) the concrete next experiments are now logged in `backlog.md` as the metadata-baseline pass plus the top-risk-bucket usefulness check on empirical loop rate, cap-hit rate, and accuracy.
- 2026-03-20 05:36 UTC: rebased the prompt-profile branch onto the newer upstream `main` state, resolved the docs-layer merge drift, and extended the implementation so repeated-rollout builds can supervise either direct `s_0.9` or `mean_relative_length` while emitting one reusable `diagnostics/prompt_rollout_archive.jsonl` bundle per dataset build.
- 2026-03-21 17:18 UTC: the first real GPQA `s_0.9` pilot finished end to end on the 3-GPU path (`37m22s`) and materially changed the recommendation. The runtime path is fine, but the target is not: on this `32 / 16` pilot, `s_0.9` equals `p(max_length_hit)` for every prompt, the probe's eval Brier (`0.03456`) is slightly worse than a constant baseline (`0.03434`), and eval Spearman is negative (`-0.2799`). The next run should therefore keep the same prefill/data-view design but switch the main head to `mean_relative_length` (or lower the tail threshold) instead of treating `s_0.9` as the settled first objective.
- 2026-03-21 17:24 UTC: reused the finished rollout archive and prefill shards to train `mean_relative_length` on the exact same prompts with no second rollout. This confirms the direction but not success yet: the best regression run improves eval Spearman to `+0.0647`, but its eval MSE/MAE (`0.0503 / 0.1549`) still trail both a constant baseline (`0.0311 / 0.1342`) and a prompt-length-only linear fit (`0.0422 / 0.1425`). The next experiment therefore needs a denser target plus a larger prompt count, not only a label swap on the same 48-prompt slice.
- 2026-03-21 23:53 UTC: generated the first corrected activation-based visualization from the saved GPQA prompt-profile pilot rather than from rollout text. The exporter now joins `manifest.json`, saved prefill shards, prompt-profile JSONLs, and `prompt_rollout_archive.jsonl` into one prompt/rollout projection table; the rendered PCA panels show that correctness is the clearest broad gradient in last-layer prefill space, while `max_length` hits remain visible but fragmented across several prompt islands and loop labels stay broader and more mixed than cap hits.
- 2026-03-22 00:28 UTC: moved from recommendation to execution on the `majority_s_0.5`, `n=4` all-dataset pilot. The builder/launcher now has a direct prompt-majority binary head plus `LiveCodeBench` prompt loading, the branch head advanced through commits `90bfb7f` and `0fcd9ce`, and the full five-dataset job chain (`1492`-`1505`) is queued on `tianhaowang-gpu0` with a hard `2`-GPU cap per job on the pinned `3,5` pair.
- 2026-03-22 00:52 UTC: added the dedicated prompt-level projection path requested in-thread. `export_prompt_profile_projection.py` now keeps one point per prompt, recomputes prompt-majority and `s_t` labels directly from `diagnostics/prompt_rollout_archive.jsonl`, and scores each label against one unsupervised KMeans partition on the shared PCA plane. `run_prompt_profile_projection.sbatch` launches the whole build/export path on `1` GPU. A saved-GPQA validation export already shows that correctness aligns with the prompt clusters much more than prompt-majority loop / cap labels, while `s_0.5` and `s_0.6` are almost unchanged on that slice. The corrected all-dataset serial chain is now `1539`-`1543`.
- 2026-03-22 05:02 UTC: the resumed one-GPU probe queue exposed a second hidden config bug: prompt-profile builders were on `LOOP_K=20` while `run_probe_train_e2e.sbatch` still defaulted to `LOOP_K=10`, so the supposed reuse jobs were silently regenerating rollout bundles. The live queue was rebuilt with explicit `LOOP_K=20` throughout, one stale `s09` scratch worktree was pruned, and the corrected GPQA last-layer run immediately reused the cached dataset and finished in `17s` with `PR-AUC 0.2262`, `ROC-AUC 0.8684`, `F1 0.6571`, `acc 0.85` on the `40`-prompt split.
- 2026-03-22 05:04 UTC: the corrected GPQA ensemble finished too, so the first arm-to-arm comparison on the same reused dataset now exists. On the `40`-prompt GPQA split (`5%` positives), last-layer reached `PR-AUC 0.2262` / `ROC-AUC 0.8684` / `macro-F1 0.6571`, while the per-layer vote ensemble reached `PR-AUC 0.6667` / `ROC-AUC 0.9671` / `macro-F1 0.5671`. The ensemble is materially better on ranking metrics, while the last-layer arm still looks better on thresholded metrics in the tiny-positive regime.
- 2026-03-22 05:21 UTC: the projection/export path now writes the leakage baselines directly into each prompt-profile summary rather than leaving them as an external reminder. `export_prompt_profile_projection.py` now scores prompt-token-count and effective-budget controls using train-chosen orientation and threshold, and the patched exporter was synced onto the live remote worktrees before the running `AIME` / `MATH-500` jobs reach their export phase. The refreshed `GPQA` summary makes one point explicit: on this `max_tokens=30000` surface, effective budget is constant at `30000` for every prompt and therefore useless as a ranking baseline, while prompt length is weak (`test PR-AUC 0.0817`, `ROC-AUC 0.5526`) compared with both finished probes.
- 2026-03-22 05:42 UTC: the resumed sweep was still leaving GPUs `3` and `5` idle even though `MMLU-Pro` and `LiveCodeBench` were independent of the live `AIME` / `MATH-500` builders. I split those datasets onto their own pinned lanes, hit one immediate launch-surface mistake (`1615` / `1618` failed because the ad hoc submissions missed `CONDA_ENV=swe311`, and the dependent train jobs still carried the launcher default `--gres=gpu:8`), then corrected it in the next requeue. The live node is now a real four-lane run: `1561` (`AIME`) on GPU `1`, `1609` (`MATH-500`) on GPU `0`, `1621` (`MMLU-Pro`) on GPU `3`, and `1624` (`LiveCodeBench`) on GPU `5`; `MMLU-Pro` is already through train prefill and into test prefill, while `LiveCodeBench` is already at `350 / 640` train-prefill prompts on the conservative batch-`2` path.
- 2026-03-22 05:47 UTC: added a small analysis helper for the live prompt-majority pilot, `scripts/summarize_prompt_majority_pilot.py`. It scans one output root for `prompt_profile_projection_*` bundles plus matching `prompt_majority05_*_{last_layer,ensemble}_seed*` runs, then emits one dataset/seed summary row carrying the leakage baselines and the learned probe metrics together. A real GPQA fixture check already reproduces the known `PR-AUC` table (`0.0817` prompt length, `0.2262` last-layer, `0.6667` ensemble), so once the long builders land the cross-dataset scoreboard can be regenerated without another manual merge pass.
- 2026-03-22 05:53 UTC: the first GPU-`5` `LiveCodeBench` retry (`1624`) turned out not to be a resource problem. It finished all prefill (`640 / 160`) and then failed exactly at grouped-rollout startup because I had lowered `MAX_NUM_SEQS` to `2` while still using `NUM_GENERATIONS=4`; repeated-rollout mode enforces `max_num_seqs >= num_generations`. I canceled the dead dependent tails, removed the partial LCB output roots, and requeued the lane as `1627 -> 1629` with `MAX_NUM_SEQS=4` restored and the safer `PREFILL_BATCH_SIZE=2` kept. The overall four-lane node shape is unchanged; only the GPU-`5` lane was recycled.
- 2026-03-22 06:44 UTC: consulted Athena in deep mode on the exact next-objective question, with the prompt-profile docs, the refreshed rollout-stat report, the GPQA `s_0.9` recommendation note, the all-dataset prompt-majority note, and `src/loop_probe/labeling.py` attached. Key outcome at that stage: ship direct prompt-level loop probability `p_loop` first, keep `mean_relative_length` as the best zero-plumbing fallback, and leave `p_cap` as a diagnostic companion rather than the main head. I then promoted that recommendation into code by exposing `p_loop` and `p_cap` as trainable prompt-profile probability targets in the builder/docs, so the next run can call `build_probe_dataset.py --target-kind probability --profile-target p_loop` directly. Consult log: `.agent/runtime/consult_history/1773870804.162569.jsonl`.
- 2026-03-22 07:30 UTC: added `scripts/relabel_prompt_profile_dataset.py` so finished prompt-profile datasets can be re-targeted from their saved `diagnostics/prompt_rollout_archive.jsonl` plus prefill shards without a second rollout or feature pass. I used it immediately on the finished `GPQA` bundle to compare direct heads on the exact same prompts and activations. This was the first clear sign that the answer was no longer “`p_loop` first”: on the same archive, `mean_relative_length` was the more stable useful head while `p_loop` stayed cleaner but more selector-sensitive.
- 2026-03-22 07:54 UTC: ran the same archive-relabel comparison on the finished `AIME` seed-`0` prompt-profile bundle too, using CPU-only probe fits against the saved repeated-rollout archive so no extra GPU slot or rollout pass was needed. That kept `mean_relative_length` as the steadier utility head but showed that `p_loop` was healthy enough to stay in the default bundle instead of being dropped after the noisy GPQA pilot.
- 2026-03-22 08:22 UTC: consulted Athena again in deep mode after the `AIME` same-archive relabel landed. Final outcome from that sequence: keep the objective ranking as `mean_relative_length` first for shipped utility and `p_loop` second as the failure-prox companion, and stop using plain loss-best checkpointing as the only shipped selector. Consult log: `.agent/runtime/consult_history/1773870804.162569.jsonl`.
- 2026-03-23 03:37 UTC: fixed a prompt-field bug in `scripts/export_prompt_profile_projection.py` that only affected `MATH-500` correctness reconstruction during export, not the underlying prompt-profile dataset build. With that repair in place, I backfilled the finished `MATH-500` projection export and its prompt-majority probes from the saved bundle instead of rerolling the model.
- 2026-03-23 03:44 UTC: implemented the selector change in `scripts/train_probe.py` rather than leaving it as a note. The trainer now writes `best_loss.pt`, `best_rank.pt`, `best_loss_metrics.json`, and `best_rank_metrics.json`, with `best.pt` / `best_metrics.json` preserved as backward-compatible aliases to the loss-best checkpoint. I also reran the same-archive direct-head controls under that updated surface for `GPQA`, `AIME`, `MMLU-Pro`, and `MATH-500`. The standardized four-dataset table kept the same recommendation: `mean_relative_length` is still the safest single shipped utility head, `p_loop` is the default companion head, and `majority_s_0.5` remains control-only.
- 2026-03-23 03:57 UTC: the only unfinished dataset is now `LiveCodeBench`. The earlier 2-GPU rerun was too close to the wall-clock limit at `MAX_NUM_SEQS=8`, so I canceled it and relaunched the same `640 / 160`, `n = 4` surface as job `1668` with `MAX_NUM_SEQS=16`. The replacement run is healthy and both GPUs are saturated.
- 2026-03-23 04:20 UTC: consulted Athena again in deep mode after the standardized four-dataset table was in and `LiveCodeBench` was the only unfinished dataset. The consult question was whether there was any better scalar shipped head than the current `mean_relative_length` + `p_loop` bundle under the existing prompt-profile codebase constraints. Athena suggested one archive-derived candidate worth testing, `loop_budget_share = E[1{loop} * (L / E)]`, because it keeps loop severity without collapsing to `p_cap`. Consult log: `.agent/runtime/consult_history/1773870804.162569.jsonl`.
- 2026-03-23 04:52 UTC: tested one extra archive-derived scalar before closing the objective recommendation: `loop_budget_share = E[1{loop} * (L / E)]`. I added it to the local labeling / relabel / dataset-build paths, synced the patch onto the remote cot-loop worktrees, and ran a CPU-only archive sweep (`1689`) on the finished `GPQA`, `AIME`, `MATH-500`, and `MMLU-Pro` bundles. Result: it stays interesting on `AIME`, but it is weaker than `mean_relative_length` on `GPQA`, brittle on `MATH-500`, and fails on `MMLU-Pro`, so it should remain an auxiliary severity-weighted head rather than replacing the shipped `mean_relative_length` + companion `p_loop` bundle.
- 2026-03-23 04:52 UTC: while extending the pending `LiveCodeBench` follow-up to include `loop_budget_share`, I found that the original dependency script (`1675`) was malformed and would have failed after `1668`. I canceled it before start and requeued the corrected `LiveCodeBench` follow-up as `1688`, still depending on `1668` but now including `loop_budget_share` alongside `p_loop` and `mean_relative_length`.
- 2026-03-23 05:52 UTC: one more queue bug was still hiding in the `LiveCodeBench` tail. The supposedly corrected `1688` script on the node still contained the malformed inline Python summary block and had silently dropped the `loop_budget_share` branch. Before `1668` finished, I synced a durable `scripts/summarize_prompt_profile_followup.py` helper onto the remote cot-loop worktree, canceled `1688` while it was still dependency-pending, and replaced it with `1693` behind the same `afterok:1668` gate. `1693` now runs the majority controls, all three direct heads (`p_loop`, `mean_relative_length`, `loop_budget_share`), and the compact JSON summary under one corrected script. One additional compatibility fix went in immediately after that dry run: because the current prompt-projection exporter does not surface `loop_budget_share` in `prompt_projection.csv`, the helper now records that prompt-length-coupling field as `null` instead of failing during the final summary write.
- 2026-03-23 06:42 UTC: consulted Athena again in deep mode after the four finished direct-head datasets had stabilized and `LiveCodeBench` was the only unresolved lane. That consult did not change the current head ranking: it kept `mean_relative_length` as the one shipped utility head and `p_loop` as the companion. The useful sharpening was around evaluation and fallback policy: the mandatory non-activation baselines are now `prompt_length`, `effective_budget`, and their joint baseline; `best_rank` remains the checkpoint to ship; and if `LiveCodeBench` contradicts the current ordering enough to justify one more binary-head test, direct `p_cap` is the next head to reopen rather than `loop_budget_share` or another `s_t` sweep. Consult log: `.agent/runtime/consult_history/1773870804.162569.jsonl`.
- 2026-03-24 07:41 UTC: closed the last unresolved `LiveCodeBench` gap. The expensive rollout had already completed successfully as `1668`; the reason the thread never got a final table was that the cheap dependent follow-up `1693` died in `2s` with `WANDB_API_KEY not found`. I reran the same follow-up as `1725` with the required env present under offline logging, and it completed in `1m08s`, writing `lcb_followup_summary.json`. The fifth dataset confirms the existing ranking rather than changing it: ensemble `mean_relative_length` reaches `Spearman 0.751`, ensemble `p_loop` reaches `Spearman 0.478` with `top-20% capture 0.463`, and ensemble `loop_budget_share` only reaches `Spearman 0.180`. Prompt length is still notably weaker on the same archive (`Spearman 0.405` against `mean_relative_length`, `0.191` against `p_loop`). The open item is now the joint `prompt_length + effective_budget` baseline, not another unfinished dataset.
- 2026-03-25 00:23 UTC: collaborator feedback exposed that the docs were still blurring three different questions: what the target actually is, what "prompt-length baseline" means, and what `Spearman` / `top-20% capture` mean operationally. I added a dedicated evaluation-contract note and refreshed the roadmap/backlog/docs index so the current state is explicit: `majority_s_0.5` can still be operationally useful for catching degenerate prompts, but on `AIME` it is too prompt-length-shaped to be the main activation-lift claim; the five-dataset continuous-head table still lacks trained metadata-only baselines; and the next concrete experiments are (1) trained `prompt_length`, `effective_budget`, and joint metadata baselines for the continuous heads, plus (2) a direct top-risk-bucket comparison of `majority_s_0.5`, `p_loop`, and `mean_relative_length` on empirical loop rate, cap-hit rate, and accuracy.

## Milestone 5 - Deployment readiness
Status: future (set 2026-03-13 13:05 UTC)
Success criteria:
- Recommend a default feature view and probe configuration for routine use.
- Document performance tradeoffs, expected failure modes, and cost profile.

Activity log:
- 2026-04-06 10:11 UTC: refreshed the live Qwen same-family checkpoint again directly from the node before touching the thread draft. There is still only one finished durable JSON row (`bound50_20260406_gpu3/math500.json`), but the remaining per-dataset collectors are materially farther along and still clearly alive: `AIME 43 / 50`, `GPQA 31 / 50`, `MMLU-Pro 39 / 50`, and `LiveCodeBench 26 / 50`. I also wrote down the stronger liveness evidence instead of only the counts: all four `collect_model_stats.py` workers are still running, and their `run.log` files were updated between `10:04 UTC` and `10:10 UTC`, so this is slow decode rather than a silent stall.
- 2026-04-06 09:45 UTC: refreshed the live Qwen same-family checkpoint again from the node and cleaned the note surfaces where they had started talking like a finished bad base `LiveCodeBench` row already existed. That is still not true: the only durable Qwen base JSON row remains `bound50_20260406_gpu3/math500.json`. What changed is the live continuation state and the precision of the written read. The current prompt counts are now `AIME 37 / 50`, `GPQA 26 / 50`, `MMLU-Pro 33 / 50`, and `LiveCodeBench 18 / 50`. I also tightened the top-level note snapshot to match the finished `MATH-500` evidence rather than only the tiny MCQ probe: Qwen base raw often loops on the MCQ answer-format instruction tail, but the finished long-horizon `MATH-500` row also shows a smaller real math loop / cap regime, so the Qwen base pathology is not MCQ-only.
- 2026-04-06 09:28 UTC: the tiny Qwen base `LiveCodeBench release_v6` style probe is now finished, so the old LM-style caveat is sharper than it was at `09:10 UTC`. On the same `2`-prompt / `1`-generation slice, `GenericBase` and `CodeQwenInstruct` both came back at `2 / 2` looped, `2 / 2` max-length-hit, and `pass@1 = 0.0`. What changes is the failure text, not the top-line result: `GenericBase` starts with plausible code and then drifts into repeated problem-text fragments, while `CodeQwenInstruct` collapses almost immediately into repeated `# YOUR CODE HERE`. So the weak base `LiveCodeBench` read is not explained by LM style alone. I also refreshed the live node checkpoint again: the only finished same-family Qwen JSON row is still `MATH-500`, while the direct collectors are now at `AIME 29 / 50`, `GPQA 23 / 50`, `MMLU-Pro 28 / 50`, and `LiveCodeBench 15 / 50`.
- 2026-04-06 09:10 UTC: refreshed the live Qwen same-family checkpoint again from the node and corrected the doc surfaces so they stop implying the active base control is all in one serialized root. The only finished JSON row is still `bound50_20260406_gpu3/math500.json`; the continuation is the dedicated per-dataset collector root `parallel_by_dataset_20260406/`. Current prompt counts are `AIME 25 / 50`, `GPQA 18 / 50`, `MMLU-Pro 22 / 50`, and `LiveCodeBench 11 / 50`. I also launched a tiny Qwen base `LiveCodeBench` style probe on the now-free GPU `3` so we can compare the active `GenericBase` surface against the old `CodeQwenInstruct` string style on the same `release_v6` family before the full base `LiveCodeBench` row lands.
- 2026-04-06 09:03 UTC: tightened the durable Qwen same-family reference instead of leaving it half in thread memory. The working note now anchors the old instruct-side reference to the actual saved files under `outputs/qwen3_1p7b_rollout_stats_v2_temp0p2_gen10/`, spells out the saved five-dataset instruct rows, and makes one provenance gap explicit: that repaired v2 bundle records `temperature`, `num_generations`, `max_tokens`, and `max_model_len`, but it does not actually persist `top_p` / `top_k`. I also refreshed the live node checkpoint from the direct run logs. The base control still has only one finished JSON row (`MATH-500`), while the remaining direct collectors are still advancing at `AIME 24 / 50`, `GPQA 17 / 50`, `MMLU-Pro 22 / 50`, and `LiveCodeBench 11 / 50`.
- 2026-04-06 08:48 UTC: refreshed the active Qwen same-family control directly from the node so the status surface stops pretending the run is either done or stalled. The only finished JSON row is still `MATH-500`; the other four datasets remain live as direct pinned collectors rather than Slurm jobs. Latest prompt counts from the actual logs are `AIME 19/50`, `GPQA 13/50`, `MMLU-Pro 15/50`, and `LiveCodeBench 7/50`. That means the same-family control is scientifically informative already, but not yet a finished five-dataset table.
- 2026-04-06 08:16 UTC: re-checked the live Qwen base control directly from the node instead of inferring from old Slack counts. The remaining four dataset collectors are still live as direct pinned `collect_model_stats.py` processes rather than Slurm jobs, with the latest log checkpoint at `AIME 13/50`, `GPQA 7/50`, `MMLU-Pro 8/50`, and `LiveCodeBench 4/50`. That same check made the same-family comparison stronger than "Qwen base also loops": the finished base `MATH-500` row is already worse than the old Qwen instruct v2 `MATH-500` reference on both loop rate (`0.038` vs `0.0294`) and max-length-hit rate (`0.044` vs `0.0146`), even though the base row is much shorter on average (`1893.6` vs `6227.6`). The failure-mode split from the saved probes still holds: Qwen base raw degenerates on MCQ mainly by repeating the instruction tail itself, while OLMo2 base repeats derivations or synthetic-junk patterns.
- 2026-04-06 07:56 UTC: the first full long-horizon Qwen base row is now on disk, so the control has moved beyond probe text. Under the old-v2-style sampler with the base checkpoint's real `32768` limit, `Qwen/Qwen3-1.7B-Base` on raw `MATH-500` finished `50` prompts / `500` rollouts at `243/500` correct, `19/500` looped, `22/500` max-length-hit, with every looped rollout also hitting max length. That means the longer-horizon Qwen base control now shows real math degeneration too, not only the MCQ instruction-tail failure from the tiny probe. After `math500.json` was written, I killed the old serialized follow-on on GPU `3` because it had automatically rolled into a duplicate `AIME` run; the live surface is now one finished `MATH-500` row plus four dedicated collectors for `AIME`, `GPQA`, `MMLU-Pro`, and `LiveCodeBench`.
- 2026-04-06 07:52 UTC: tightened the active degeneration-origin control from "live queue exists" to "we know what the failures look like and what contract caveat still remains." I pulled the saved remote text probes directly instead of paraphrasing the earlier thread. The Qwen base raw probe confirms that the long MCQ failures are instruction-tail loops from the raw answer-format surface itself, with prefixes like `Do not output anything else. Do not explain your answer.` or `Do not output anything that is not JSON.` repeated to the `~32k` cap. The OLMo2 base probe is a different object: `MATH-500` repeats derivation lines, `GPQA` flips among incompatible answer statements, and `MMLU-Pro` falls into synthetic `Input: [...]` or `None, None, ...` junk. I also re-read the saved Qwen v2 rollout bundle to pin the `LiveCodeBench` surface exactly: the old row used raw strings from `format_prompt_generation` under `CodeQwenInstruct`, whereas the current base control has to use `GenericBase`, so that row is not a literal same-LM-style replay. The live five-way Qwen base queue is now materially inside collection: `MATH-500 34/50`, `AIME 5/50`, `GPQA 2/50`, `MMLU-Pro 2/50`, `LiveCodeBench 1/50`.
- 2026-04-06 07:02 UTC: Wangzhi pushed specifically on the unanswered "why" behind the strong metadata predictors, so I did a second mechanism-focused pass instead of just re-citing the combined audit. I built `outputs/prompt_profile_metadata_mechanism_20260406/metadata_mechanism_summary.json`, which adds two new pieces of evidence from the saved archives: (1) cross-head linkage showing that ground-truth `mean_relative_length` predicts `majority_s_0.5` extremely well on the binary test sets (`PR-AUC = 1.000` on `AIME` and `GPQA`, `0.972` on `LiveCodeBench`), and (2) a simple prompt-shape-model check showing that the prompt-only signal is real but benchmark-specific and nonlinear rather than one clean additive metadata ceiling. I then wrote the collaborator-facing mechanism note `docs/prompt-profile-metadata-mechanism-2026-04-06.md` plus `outputs/prompt_profile_metadata_mechanism_20260406/prompt_profile_metadata_mechanism_20260406.pdf`. Athena was consulted again in deep mode, one turn, against a code-grounded context bundle built from the target definitions, the new mechanism summary, and representative prompt examples. Athena agreed with the local read but sharpened the claim: the surface features are mostly proxies for latent prompt family / expected derivation verbosity, the binary head is largely a thresholded long-completion object rather than a loop-probability object, the quoted `MMLU-Pro newline_count` win is mostly a tiny-prevalence artifact, and the cheapest decisive next check is a residualized conditional-lift audit on the natural regression head rather than another binary-only analysis. Consult log: `.agent/runtime/consult_history/1775257834.444509.jsonl`.
- 2026-04-05 02:14 UTC: bundled the current prompt-profile surface into one self-contained regression-plus-binary audit and asked Athena to reason over the full code path rather than only the metrics tables. The new note `docs/prompt-profile-combined-audit-2026-04-05.md` and bundle `outputs/prompt_profile_combined_audit_20260405/` now keep four things together: the natural-split regression rerun that reproduced locked run `2043`, the current balanced-binary recommendation (`ensemble h256 d1`), a cheap prompt-stat audit from the saved prompt archives, and Athena's audit of the build / label / train / summarize code path. The main correction is narrower than "metadata is suspiciously strong": on this fixed-budget surface the reported metadata baseline is only prompt length, and cheap prompt-shape features such as newline count, dollar count, and character length already explain much of the same signal. Athena's read was that the code path does not show one hidden implementation bug driving the result; the missing control is a stronger prompt-shape baseline plus residualized evaluation. Consult log: `.agent/runtime/consult_history/1775257834.444509.jsonl`.
- 2026-04-05 00:54 UTC: reran the canonical regression object directly instead of only arguing for it. Slurm `2215` retrained `mean_relative_length` on the natural prompt-disjoint train/test split with natural sampling from the current PR head, using the saved April shared archives and a new regression-only wrapper `slurm/run_prompt_profile_natural_regression_rerun.sbatch`. The copied summary matched the original locked `2043` regression ledger exactly, with max absolute difference `0.0` across the prompt-only metadata baselines plus the `ensemble` and `last_layer` aggregates at both `best_loss` and `best_rank`. I also tightened the PR code surface around that rerun: regression-only summaries now stay marked `complete` when binary outputs are intentionally absent, the summary launcher now forwards `--expected-targets` correctly, mixed legacy/v2 rollout bundles no longer fail validation during report repair, and binary runs no longer apply BCE class weighting on top of an already balanced train sampler.
- 2026-04-04 07:15 UTC: the prompt-profile rerun surface is less ambiguous now even though the actual run is still blocked. The code path from commit `dfa9ab8` already let regression reuse the balanced binary prompt subset, but the repo did not yet have a clean 2-GPU launch surface for that object. I added `slurm/run_prompt_profile_balanced_regression_retrain.sbatch`, which reads the saved shared `mean_relative_length` archives from the locked April root, reuses the balanced `majority_s_0.5` train/test sample IDs from that same root, writes the rerun into a fresh regression-only output tree, and then emits a summary ledger. I also corrected `backlog.md` so it no longer claims the balanced follow-up is binary-only. The remaining blocker is still infrastructure rather than project code: `tianhaowang-gpu0` accepts TCP on `22`, but SSH still times out during banner exchange, so I still cannot reach Slurm to submit the balanced regression rerun itself.
- 2026-04-04 06:55 UTC: collaborator pushed directly on the weird OLMo numbers and asked whether there is a smaller model size to use instead. I re-read the saved bounded note against the current output summary and the public Hugging Face model cards. Two durable corrections now belong in the project docs: first, the strange rows should stay in the "audit before claim" bucket rather than the "either trust or discard" bucket, because `RLVR / MMLU-Pro = 0 / 80` with mean length `6.15` and `SFT / LiveCodeBench = 0 / 80` still look more like surface-level mismatch than stable stage capability, while the same bounded bundle already shows nonzero native `LiveCodeBench` `pass@1 = 0.10` and `pass@10 = 0.375`; second, there is no smaller same-family OLMo 3 instruct progression to swap in. The public OLMo 3 release exposes only `7B` and `32B`, so the smallest public fallback progression is actually OLMo 2 `1B` (`base -> SFT -> RLVR1 -> Instruct`), which should be treated as a family-change debug object only after the current OLMo 3 surface is audited.
- 2026-04-04 06:26 UTC: the narrowed instruct-only queue is now finished. Under the bounded `temperature=0.1`, `top_p=0.95`, `top_k=-1`, `num_generations=10`, `max_tokens=max_model_len=40960` contract on `8` prompts per dataset, SFT finished with light degeneracy only on `AIME` and `MMLU-Pro` (`1 / 80` looped and `1 / 80` max-length-hit on each), while RLVR stayed loop-free and max-length-hit-free on every collected dataset. The finished instruct-stage bundle is now written into `understand-where-loop-and-max-length-come-from.md`, but the read is still provisional rather than settled: `RLVR / MMLU-Pro` came back at `0 / 80` correct with mean length `6.15`, which looks more like an answer-format or grading mismatch than a stable stage-level capability read, and `LiveCodeBench` remains on its own raw benchmark interface rather than the MCQ chat surface. The unresolved execution objects are now the deferred base stage plus this sanity check on the suspect instruct-side datasets.
- 2026-04-04 05:49 UTC: collaborator narrowed the live thread target again after seeing the base runtime gap: skip base for now and finish collecting only SFT / RLVR stats in this thread. I canceled the base chain `2091`-`2095`, leaving the instruct-stage queue as `2097`-`2100` for SFT and `2102`-`2105` for RLVR across `AIME -> GPQA -> MMLU-Pro -> LiveCodeBench`. That means the thread deliverable is now the instruct-stage rollout-stat bundle first, with base deferred as the current runtime bottleneck rather than the next best use of node time.
- 2026-04-04 05:39 UTC: Wangzhi tightened the execution contract one more time: stop treating `MATH-500` as the only current object, keep collecting across datasets, and drop the rollout temperature from `0.2` to `0.1`. I canceled the stale base-only `0.2` rerun (`2088`) and replaced it with a bounded five-dataset dependency chain under `/data/scratch/murphy/outputs/cot-loop-detection/olmo3_degeneration_origin_progression/bound8_temp0p1_gen10_ctx40960_topkneg1/`. The live jobs are `2091`-`2095` for base, `2096`-`2100` for SFT, and `2101`-`2105` for RLVR, all on the shared decode contract `temperature=0.1`, `top_p=0.95`, `top_k=-1`, `num_generations=10`, `max_tokens=max_model_len=40960`, bounded to `8` prompts per dataset. Prompt surfaces stay native where that distinction already proved real (`base/raw`, `SFT+RLVR/chat_template`), while `LiveCodeBench` stays raw-string for every stage because the benchmark prompt builder itself is raw. At this checkpoint, SFT already cleared `MATH-500` and rolled into `AIME`; RLVR and base are still on the first `MATH-500` leg, with base still the obvious runtime long pole.
- 2026-04-04 04:36 UTC: expanded the locked full-train note into a self-contained report after collaborator feedback that the earlier revision still assumed too much thread context. The revised Markdown/PDF now carries the frozen run contract, dataset table, exact target/view/baseline definitions, full regression tables (`top_10p_capture`, `top_20p_capture`, `RMSE`, `Spearman`), full binary ranking/threshold tables, and the final recommendation in one place.
- 2026-04-04 04:25 UTC: collaborator follow-up settled the remaining metric ambiguity on the fresh locked full-train note. The durable result surface now treats `mean_relative_length` as a screening score first, not a calibrated regressor first: the revised note and PDF keep the already-frozen `best_loss` checkpoint but move the headline regression read to held-out `top_20p_capture`, keep `RMSE` as calibration context, and demote `Spearman` to tertiary diagnostic status. That changes the honest continuous-head summary from "activation only beats prompt length on `2 / 5` datasets by RMSE" to the more operational "activation beats prompt length on `3 / 5` datasets by top-20 capture (`GPQA`, `MMLU-Pro`, `LiveCodeBench`), while `AIME` and `MATH-500` remain prompt-length-shaped."
- 2026-04-03 23:33 UTC: Wangzhi's follow-up made the remaining drift obvious: the new root note still risked sounding like a prompt-profile label debate when it should have been anchored on the older rollout-statistics line. I re-read `upstream/main`, the repaired Qwen3 common-policy bundle, and the rollout-stats audit artifacts, then rewrote `understand-where-loop-and-max-length-come-from.md` so "collect stats like we did with Qwen3" now points to the literal collector/report surface and the actual metric family (`loop_fraction`, `max_length_hit_fraction`, the two overlap conditionals, length metrics, and success conditionals). I also split the doc index cleanly: the repo-root note is the OLMo progression plan, while `docs/understand-where-loop-and-max-length-come-from.md` is only the definitions appendix.
- 2026-04-03 23:17 UTC: the deployment-readiness surface is a bit less hand-wavy now. The locked run is no longer only a plan note plus example commands; `scripts/run_prompt_profile_full_train.py` and `scripts/summarize_prompt_profile_full_train.py` now define the canonical execution and reporting path for the current pair.
- 2026-04-03 23:15 UTC: ported Wangzhi's new root-level planning note `understand-where-loop-and-max-length-come-from.md` from `upstream/main` onto the live prompt-profile branch and expanded it just enough to make it executable against the current repo. The note fixes the actual research object: stop treating this as another prompt-profile target-choice discussion and instead use the repaired rollout-statistics module to test where degenerate rollouts enter the model family along `Olmo-3-7B -> Olmo-3-7B-Instruct-SFT -> Olmo-3-7B-Instruct`. It now points directly at the current collection path (`scripts/collect_model_stats.py`, `scripts/build_cross_dataset_rollout_report.py`), keeps the benchmark family aligned with the existing Qwen3 common-policy bundle, separates Sankey-within-checkpoint from line-plot-across-checkpoint visualization, and states the decision rule in terms of whether degeneracy rises at SFT or RLVR rather than in terms of prompt-profile proxy predictability.
- 2026-04-03 23:15 UTC: added `docs/understand-where-loop-and-max-length-come-from.md` to pin the exact semantics behind the repo's saved `loop` and `max_length` signals. The note does four things in one place: (1) names the actual loop detector (`n=30`, `k=20` repeated n-gram on generated token IDs), (2) separates the prompt-profile cap object (`cap_hit` / `p_cap`, based on `effective_max_tokens`) from the older rollout-stat `max_length_hit` object (prompt plus generation reaching `max_model_len`), (3) explains how prompt-level targets such as `p_loop`, `p_cap`, `mean_relative_length`, and `majority_s_0.5` are built from those rollout-level events, and (4) records the saved overlap read that cap hits are the narrower terminal event while loops are broader. This is a documentation cleanup rather than a new result, but it closes a real interpretation gap that was still causing thread drift.
- 2026-04-02 23:04 UTC: the planning deliverable is now real instead of implied. The repo now carries the full-train execution note and PDF for the locked `mean_relative_length` / `majority_s_0.5` pair, so the next gating item is the run itself rather than another target-choice write-up.
- 2026-04-02 11:34 UTC: the next execution surface is unchanged, but the review-path diagnosis is cleaner now. Maintenance confirmed that `review_project.sh --base main` can sync against the real `upstream/main` base on this repo, so the older fork-base blocker note should not be carried forward. Upstream PR `#7` is now at head `6edc889` with `16` unresolved non-outdated review threads, and the branch remains pending because the bounded local review still timed out before a terminal verdict.
- 2026-04-01 11:18 UTC: the target-choice debate is no longer the gating item for the next run. Wangzhi explicitly locked `mean_relative_length` plus `majority_s_0.5` as the next full-train pair and asked for a PDF train plan. The repo docs now treat that pair as the active execution surface, with `p_loop` left in the analysis lane unless a later prospective run justifies reopening it as the main train target.
- 2026-03-30 10:55 UTC: corrected the objective selector itself after collaborator pushback that the label choice should be based on how well the target can actually be predicted, not on a downstream bucket-enrichment diagnostic. I re-read the saved five-dataset control bundle with that criterion fixed. The durable notes now separate binary and regression instead of forcing one winner: `mean_relative_length` is the strongest current regression target by held-out fit (best-spearman average about `0.661` versus `0.372` for `p_loop`), `majority_s_0.5` remains the strongest finished binary label surface, and `p_loop` is now explicitly downgraded from "chosen objective" to "loop-specific target that still wins the old bucket test but is not yet proved to be the most learnable target." The plain-language note, technical note, backlog, and docs index were all refreshed so future reloads do not inherit the old selection rule.
- 2026-03-30 10:15 UTC: corrected the plain-language framing after collaborator pushback that I had quietly changed the project object from "choose the right prompt-level target" into "rank prompts." The durable fix is in the note itself, not only in thread prose. `docs/prompt-profile-plain-language-2026-03-30.md` now states explicitly that the project goal is target choice under fixed model/policy; the `top 20%` bucket is only one common held-out evaluation slice; `flagged top-risk bucket` and `loop-rate enrichment` are both defined in plain words; and `loop-rate enrichment` is justified only as a normalization/comparison device across datasets with different base loop rates. This does not change the empirical decision from the finished control bundle: `p_loop` is still the best main target, `mean_relative_length` is still secondary, and `majority_s_0.5` stays in the control lane.
- 2026-03-30 09:14 UTC: wrote a plain-language objective note plus PDF companion after collaborator feedback that the current explanation was still too esoteric. The new note (`docs/prompt-profile-plain-language-2026-03-30.md`) keeps the object fixed in plain terms: what the project goal is, what `p_loop` / `p_cap` / `majority_s_0.5` / `mean_relative_length` mean, what was actually trained in the finished bundle comparison, why the held-out bucket test promotes `p_loop`, and what is still unresolved. The readable PDF lives in `outputs/prompt_profile_plain_language_20260330/`.
